1. 基本ルール & 言語設定
使用言語: 対話、ドキュメント、コミットメッセージ、PR説明文を含め、すべての返答・出力は日本語で行うこと。（コード内の変数名、関数名、型定義などは英語/アルファベット可）
動作モード: アンソロピック（Anthropic）へのデータ提供/AI学習利用をオフにしたプライベート開発モードで動作すること。
要件の明確化: 仕様や実装方針に不明確な点がある場合は、コードの記述を開始する前に必ずインタビュー（ヒアリング）を実施し、要件を確定させてから実装に進むこと。
2. セキュリティ & 機密情報保護
機密情報の保持禁止: APIキー、パスワード、アクセスシークレット、DB接続文字列などの機密情報をコード内やメモリ/コンテキスト上に直書き・保持してはならない。
.envの扱い: 機密情報はすべて .env.local などの環境変数で管理し、.gitignore に含まれていることを必ず確認すること。
情報漏洩対策: ログ出力やエラーハンドリング処理に、個人情報や認証トークンが含まれることがないよう実装・確認すること。
3. Gitワークフロー & 開発手順
ブランチ戦略: 新機能の追加、バグ修正、機能改修ごとに必ず個別ブランチ（例: feature/xxx, fix/xxx）を作成して作業を行うこと。
事前検証（エラー検出・動作確認の徹底）:
コミットおよびプッシュの前に、必ず動作確認とビルドチェック（npm run build / tsc 等）を実施し、エラーが発生していないことを確認すること。
エラーや警告が残っている状態でのコミット・プッシュは厳禁とする。
自動化処理: エラーのない動作状態を確認後、以下の処理を実行すること。
適切なコミットメッセージを作成して git commit
リモートリポジトリへ git push
GitHub上での Pull Request (PR) の作成
マージ権限の厳守: PRを作成後、メインブランチへのマージは自動で行わず、必ず人間に承認を求めること。
4. トークン消費削減 & コード編集原則
最小限の読み込み: ファイル全体の全読み込みを避け、関連する関数・コンポーネント・該当行のみをピンポイントで読み込むこと。
最小限の修正: 既存コードの修正や改善は必要最小限の変更にとどめること。リファクタリング等のついでに行う過剰な書き換えは禁止する。
差分管理: 修正理由が明確でないコード変更を行わないこと。
5. 音声案内（オーディオ通知）仕様
アプリケーションのUIおよび開発時の対話イベントにおいて、Web Speech API (speechSynthesis) 等を活用した音声読み上げ案内を実装すること。
対象タイミング:
インタビュー（ヒアリング）発生時: ユーザーへ質問やヒアリングを行う際（例：「確認事項があります。画面の質問をご確認ください。」）
認証・再パスワード入力時: セキュリティ保護のための再認証ダイアログが表示された際（例：「パスワードの再認証が必要です。」）
処理完了時: 設定保存、書籍追加、カレンダー登録、PR作成などの主要な処理が正常終了した際（例：「処理が完了しました。」）
実装要件: 音声は日本語 (ja-JP) を指定し、ブラウザの音声合成機能を使用して外部API依存なしで実装すること。
6. 推奨プラグイン & Superpowers（拡張機能・スキル）
本プロジェクトの開発にあたり、以下のプラグイン/スキルを活用・適用すること。
Superpowers Plugin: 高度なコード分析、リファクタリング提案、自動編集機能を有効化して実装の精度を高める。
Context-Minimizer / Token-Saver Skill: トークン消費量を極小化するためのファイル検索・絞り込み表示スキルを併用する。
Git / GitHub Integration Skill: CLI経由でのブランチ切り替え、コミット、Push、PR作成の自動化手順を統合する。
7. コーディング & テスト品質規約
TypeScript & Strict Mode: 型安全性（TS Strict Mode）を担保し、any 型の使用を原則禁止とする。
品質維持: エラーログや未解決の型エラーを残したまま次のタスクに移行しないこと。
